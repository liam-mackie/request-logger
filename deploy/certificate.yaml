apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: webhooks
  namespace: request-logger
spec:
  secretName: webhooks-tls
  dnsNames:
    - webhooks.gke.beans.sh
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
